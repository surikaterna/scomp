{
  "name": "@scomp/transport-websocket-client",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "lint": "bunx tsc -b tsconfig.json --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@scomp/core": "workspace:*",
    "@scomp/transport-shared": "workspace:*",
    "@scomp/transport-websocket-shared": "workspace:*",
    "@scomp/types": "workspace:*"
  },
  "devDependencies": {}
}

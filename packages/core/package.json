{
  "name": "@scomp/core",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint configured for @scomp/core\"",
    "test": "jest --config jest.config.cjs"
  },
  "dependencies": {
    "@scomp/types": "workspace:*"
  }
}
